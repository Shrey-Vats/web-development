// create a function take that todo as input and render them

interface todoType {
    title: string,
    description: string,
    done: boolean
}

interface todoInput {
    todo: todoType
}

function render({todo}: todoInput){

}